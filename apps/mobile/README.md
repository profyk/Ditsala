# @ditsala/mobile

Not yet scaffolded. This is a Phase 2 deliverable per `docs/DITSALA_MASTER_SPEC.md` §9 (Onboarding): Expo + Expo Router + NativeWind, EAS development builds from day one (Expo Go is not a target).

This stub exists only so the pnpm workspace and root CI resolve cleanly before Phase 2 begins.
