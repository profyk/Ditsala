/**
 * Placeholder. This package's real contents are generated from the backend's
 * OpenAPI schema (see package.json "generate" script) starting once the
 * backend has stable API routes worth typing (Phase 1+). Nothing here should
 * be hand-written once generation is wired up.
 */
export {};
