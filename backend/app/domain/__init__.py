"""
Pure business logic, framework-agnostic (no FastAPI/SQLAlchemy imports here —
see docs/DITSALA_MASTER_SPEC.md §3.3). Bounded contexts (accounts, kyc,
messaging, circle, location, sos, calls) are built out starting Phase 1-2.
"""
