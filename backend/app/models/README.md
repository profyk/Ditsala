# models/

SQLAlchemy ORM models, one module per bounded context, mirroring the tables in docs/DITSALA_MASTER_SPEC.md §4. Built out in Phase 1 as Alembic migrations land — never created by hand against any environment (see §7.5).
