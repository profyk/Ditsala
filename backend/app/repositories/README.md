# repositories/

Data-access layer, one module per aggregate (users, kyc, devices, conversations, messages, circle, location, sos, calls, admin). Built out starting Phase 1 alongside the SQLAlchemy models in `models/` — see docs/DITSALA_MASTER_SPEC.md §4.
