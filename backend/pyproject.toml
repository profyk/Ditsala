[project]
name = "ditsala-backend"
version = "0.1.0"
description = "DITSALA backend — FastAPI, per docs/DITSALA_MASTER_SPEC.md §3"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic[email]>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy>=2.0",
    "alembic>=1.14",
    "asyncpg>=0.30",
    "psycopg[binary]>=3.2",
    "redis>=5.2",
    "argon2-cffi>=23.1",
    "pyjwt>=2.9",
    "structlog>=24.4",
    "httpx>=0.27",
    "aiosmtplib>=3.0",
    "boto3>=1.35",
    "websockets>=13.0",
    "pyotp>=2.10",
    "apscheduler>=3.11.3",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "ruff>=0.8",
    "mypy>=1.13",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
# Autogenerated by `alembic revision --autogenerate` — line length isn't
# worth hand-wrapping on every future migration.
"migrations/versions/*.py" = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
# Test functions/fixtures don't carry the same "public API" contract as
# app code — full annotation on every fixture is noise, not safety.
module = "app.tests.*"
disallow_untyped_defs = false

[[tool.mypy.overrides]]
# No official type stubs; boto3-stubs is a large, separate install not
# worth the weight for the two presigned-URL calls we make.
module = ["boto3.*", "botocore.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# apscheduler ships no py.typed marker / stubs as of 3.x.
module = ["apscheduler.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["app/tests"]
asyncio_mode = "auto"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
