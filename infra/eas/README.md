# infra/eas

EAS build profiles for `@ditsala/mobile`. The mobile app itself isn't scaffolded yet (Phase 2 — see `docs/DITSALA_MASTER_SPEC.md`), so `eas.json` here is a draft to drop into `apps/mobile/eas.json` (EAS's actual expected location) once `expo init` runs. Kept here in the meantime per the kickoff prompt's repository layout.
