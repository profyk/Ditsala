# React Native/Expo's native-module autolinking expects a flat, hoisted
# node_modules — pnpm's default symlinked structure breaks that. This is
# the standard, documented fix for pnpm + React Native monorepos.
node-linker=hoisted
